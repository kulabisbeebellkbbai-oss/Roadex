# Roadex

Roadex is a new project workspace. The current repository contains the initial project brief and starter structure; implementation details, build commands, and tests will be added as the product direction firms up.

## Structure

- `src/` - application source code
- `tests/` - automated tests
- `assets/` - project assets
- `docs/` - planning and contributor notes

## Current Status

No build system has been selected yet. Add install, run, test, and lint commands here when the first implementation stack is chosen.
